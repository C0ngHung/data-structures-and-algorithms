package org.conghung.datastructuresandalgorithms.dsa.queue;

public class Definition {
    // Định nghĩa - Definition:
    // Definition:
    // Queue là hàng đợi, xếp hàng và đợi
    // A queue is a waiting line where items line up and wait.
    // Như xê hàng ở 1 quán cà phê
    // Similar to lining up at a coffee shop.
    // Queue là một cấu trúc dữ liệu có thứ tự
    // A queue is an ordered data structure.
    // tuân theo nguyên tắc FIFO (First In First Out)
    // It follows FIFO (First In First Out).
    // Trong queue, thì có 2 đầu
    // A queue has two ends.
    // Đầu trước gọi là front - nơi mà ta lấy phần tử ra
    // The front is where we remove elements.
    // Đầu sau gọi là rear - nơi mà ta thêm phần tử vào
    // The rear is where we add elements.
    // Gia nhập vào queue gọi là enqueue (Thêm phần tử vào cuối hàng đợi)
    // Joining the queue is enqueue (add an element to the end).
    // Rời khỏi queue gọi là dequeue (Lấy phần tử ra từ đầu hàng đợi)
    // Leaving the queue is dequeue (remove an element from the front).
    // Phần tử được thêm vào đầu tiên sẽ được lấy ra đầu tiên
    // The element added first will be removed first.
    // Tương tự với stack, queue cũng có nhiều loại implementation khác nhau:
    // Similar to stacks, queues have multiple implementations:
    // 1. Simple Circular Array Based
    // 1. Simple Circular Array Based
    // 2. Dynamic Circular Array Based
    // 2. Dynamic Circular Array Based
    // 3. Linked List Based
    // 3. Linked List Based
    // Cho dù là loại implementation nào của loại cấu trúc dữ liệu nào thì tụi nó vẫn luon phải tuân thủ theo cái
    // Regardless of the implementation, they must adhere to the Abstract Data Type (ADT) definition.
    // Abstract Data Type (ADT) của loại cấu trúc dữ liệu đó:
    // The ADT of that data structure:
    // Queue ADT bao gồm những function sau: enqueue(), dequeue(), front() hoặc peek(), size(), isEmptyQueue()
    // Queue ADT includes: enqueue(), dequeue(), front() or peek(), size(), isEmptyQueue()
    // Khi nào nên dùng - when to use:
    // When to use:
    // - Thường được sử dụng trong tất cả những thứ phải sếp hàng đợi như: in ấn tài liệu, quản lý tiến trình trong hệ điều hành,
    // - Commonly used wherever items must queue: printing jobs, OS process management, etc.

}
